import { Slot } from 'expo-router';

// Customer-facing mobile app — future phase.
// Architecture stub: Supabase client and navigation shell are in place;
// screens will be built when mobile development begins.
export default function RootLayout() {
  return <Slot />;
}
